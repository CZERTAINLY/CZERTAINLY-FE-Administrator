// tslint:disable
/**
 * Core API
 * REST API for ILM Core
 *
 * The version of the OpenAPI document: 2.20.0-SNAPSHOT
 * Contact: ilm@omnitrust.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import type { Observable } from 'rxjs';
import type { AjaxResponse } from 'rxjs/ajax';
import { BaseAPI, throwIfNullOrUndefined, encodeURI } from '../runtime';
import type { OperationOpts, HttpHeaders, HttpQuery } from '../runtime';
import type {
    AuthenticationServiceExceptionDto,
    BaseAttributeDto,
    DiscoveryCertificateResponseDto,
    DiscoveryDetailDto,
    DiscoveryDto,
    DiscoveryResponseDto,
    DiscoverySupportedResourceDto,
    ErrorMessageDto,
    PaginationResponseDtoDiscoveryItemDto,
    PaginationResponseDtoDiscoveryMessageDto,
    Resource,
    ScheduleDiscoveryDto,
    SearchFieldDataByGroupDto,
    SearchRequestDto,
    UuidDto,
} from '../models';

export interface BulkDeleteDiscoveryRequest {
    requestBody: Array<string>;
}

export interface CancelDiscoveryRequest {
    uuid: string;
}

export interface CreateDiscoveryRequest {
    discoveryDto: DiscoveryDto;
}

export interface DeleteDiscoveryRequest {
    uuid: string;
}

export interface GetDiscoveryRequest {
    uuid: string;
}

export interface GetDiscoveryAttributesRequest {
    connectorUuid: string;
}

export interface GetDiscoveryCertificatesRequest {
    uuid: string;
    newlyDiscovered?: boolean;
    itemsPerPage?: number;
    pageNumber?: number;
}

export interface GetDiscoveryItemsRequest {
    uuid: string;
    resource?: Resource;
    newlyDiscovered?: boolean;
    itemsPerPage?: number;
    pageNumber?: number;
}

export interface GetDiscoveryResourceAttributesRequest {
    connectorUuid: string;
    resource: Resource;
}

export interface GetDiscoveryRunMessagesRequest {
    uuid: string;
    itemsPerPage?: number;
    pageNumber?: number;
}

export interface ListDiscoveriesRequest {
    searchRequestDto: SearchRequestDto;
}

export interface ListDiscoveryResourcesRequest {
    connectorUuid: string;
}

export interface ResumeDiscoveryRequest {
    uuid: string;
}

export interface ScheduleDiscoveryRequest {
    scheduleDiscoveryDto: ScheduleDiscoveryDto;
}

export interface StopDiscoveryRequest {
    uuid: string;
}

/**
 * no description
 */
export class DiscoveryManagementApi extends BaseAPI {
    /**
     * Delete Multiple Discoveries
     */
    bulkDeleteDiscovery({ requestBody }: BulkDeleteDiscoveryRequest): Observable<void>;
    bulkDeleteDiscovery({ requestBody }: BulkDeleteDiscoveryRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>>;
    bulkDeleteDiscovery({ requestBody }: BulkDeleteDiscoveryRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>> {
        throwIfNullOrUndefined(requestBody, 'requestBody', 'bulkDeleteDiscovery');

        const headers: HttpHeaders = {
            'Content-Type': 'application/json',
        };

        return this.request<void>(
            {
                url: '/v1/discoveries',
                method: 'DELETE',
                headers,
                body: requestBody,
            },
            opts?.responseOpts,
        );
    }

    /**
     * Abandons a run for good: the Discovery Provider releases it and the items already staged are never processed. Legal while the run is in progress or stopped. This is irreversible - a cancelled run cannot be resumed.
     * Cancel Discovery
     */
    cancelDiscovery({ uuid }: CancelDiscoveryRequest): Observable<void>;
    cancelDiscovery({ uuid }: CancelDiscoveryRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>>;
    cancelDiscovery({ uuid }: CancelDiscoveryRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>> {
        throwIfNullOrUndefined(uuid, 'uuid', 'cancelDiscovery');

        return this.request<void>(
            {
                url: '/v1/discoveries/{uuid}/cancel'.replace('{uuid}', encodeURI(uuid)),
                method: 'PATCH',
            },
            opts?.responseOpts,
        );
    }

    /**
     * Create Discovery
     */
    createDiscovery({ discoveryDto }: CreateDiscoveryRequest): Observable<UuidDto>;
    createDiscovery({ discoveryDto }: CreateDiscoveryRequest, opts?: OperationOpts): Observable<AjaxResponse<UuidDto>>;
    createDiscovery({ discoveryDto }: CreateDiscoveryRequest, opts?: OperationOpts): Observable<UuidDto | AjaxResponse<UuidDto>> {
        throwIfNullOrUndefined(discoveryDto, 'discoveryDto', 'createDiscovery');

        const headers: HttpHeaders = {
            'Content-Type': 'application/json',
        };

        return this.request<UuidDto>(
            {
                url: '/v1/discoveries',
                method: 'POST',
                headers,
                body: discoveryDto,
            },
            opts?.responseOpts,
        );
    }

    /**
     * Delete Discovery
     */
    deleteDiscovery({ uuid }: DeleteDiscoveryRequest): Observable<void>;
    deleteDiscovery({ uuid }: DeleteDiscoveryRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>>;
    deleteDiscovery({ uuid }: DeleteDiscoveryRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>> {
        throwIfNullOrUndefined(uuid, 'uuid', 'deleteDiscovery');

        return this.request<void>(
            {
                url: '/v1/discoveries/{uuid}'.replace('{uuid}', encodeURI(uuid)),
                method: 'DELETE',
            },
            opts?.responseOpts,
        );
    }

    /**
     * Discovery Details
     */
    getDiscovery({ uuid }: GetDiscoveryRequest): Observable<DiscoveryDetailDto>;
    getDiscovery({ uuid }: GetDiscoveryRequest, opts?: OperationOpts): Observable<AjaxResponse<DiscoveryDetailDto>>;
    getDiscovery({ uuid }: GetDiscoveryRequest, opts?: OperationOpts): Observable<DiscoveryDetailDto | AjaxResponse<DiscoveryDetailDto>> {
        throwIfNullOrUndefined(uuid, 'uuid', 'getDiscovery');

        return this.request<DiscoveryDetailDto>(
            {
                url: '/v1/discoveries/{uuid}'.replace('{uuid}', encodeURI(uuid)),
                method: 'GET',
            },
            opts?.responseOpts,
        );
    }

    /**
     * Relays the run-level attribute definitions from the Connector\'s discovery interface: the schema that configures a discovery run as a whole and applies to every resource type the run targets. Exists only for Connectors implementing discovery v2; for a v1 Connector, use the kind-scoped Connector attribute endpoint, exactly as today.
     * Get run-level Discovery Attributes from a Discovery Provider
     */
    getDiscoveryAttributes({ connectorUuid }: GetDiscoveryAttributesRequest): Observable<Array<BaseAttributeDto>>;
    getDiscoveryAttributes(
        { connectorUuid }: GetDiscoveryAttributesRequest,
        opts?: OperationOpts,
    ): Observable<AjaxResponse<Array<BaseAttributeDto>>>;
    getDiscoveryAttributes(
        { connectorUuid }: GetDiscoveryAttributesRequest,
        opts?: OperationOpts,
    ): Observable<Array<BaseAttributeDto> | AjaxResponse<Array<BaseAttributeDto>>> {
        throwIfNullOrUndefined(connectorUuid, 'connectorUuid', 'getDiscoveryAttributes');

        return this.request<Array<BaseAttributeDto>>(
            {
                url: '/v1/discoveries/{connectorUuid}/attributes'.replace('{connectorUuid}', encodeURI(connectorUuid)),
                method: 'GET',
            },
            opts?.responseOpts,
        );
    }

    /**
     * Discovery Details
     */
    getDiscoveryCertificates({
        uuid,
        newlyDiscovered,
        itemsPerPage,
        pageNumber,
    }: GetDiscoveryCertificatesRequest): Observable<DiscoveryCertificateResponseDto>;
    getDiscoveryCertificates(
        { uuid, newlyDiscovered, itemsPerPage, pageNumber }: GetDiscoveryCertificatesRequest,
        opts?: OperationOpts,
    ): Observable<AjaxResponse<DiscoveryCertificateResponseDto>>;
    getDiscoveryCertificates(
        { uuid, newlyDiscovered, itemsPerPage, pageNumber }: GetDiscoveryCertificatesRequest,
        opts?: OperationOpts,
    ): Observable<DiscoveryCertificateResponseDto | AjaxResponse<DiscoveryCertificateResponseDto>> {
        throwIfNullOrUndefined(uuid, 'uuid', 'getDiscoveryCertificates');

        const queryParams: HttpQuery = {};

        if (newlyDiscovered != null) {
            queryParams['newlyDiscovered'] = newlyDiscovered;
        }
        if (itemsPerPage != null) {
            queryParams['itemsPerPage'] = itemsPerPage;
        }
        if (pageNumber != null) {
            queryParams['pageNumber'] = pageNumber;
        }

        return this.request<DiscoveryCertificateResponseDto>(
            {
                url: '/v1/discoveries/{uuid}/certificates'.replace('{uuid}', encodeURI(uuid)),
                method: 'GET',
                queryParams,
            },
            opts?.responseOpts,
        );
    }

    /**
     * Returns one page of the items this Discovery staged, ordered by their run sequence, optionally narrowed to a single resource type. Certificates are included: a run against a v1 Discovery Provider stages certificates only, so its unfiltered listing contains exactly what the certificate listing contains, with sequence and uniqueRef synthesized by the platform (staging order and certificate fingerprint) because a v1 provider never numbered its items.
     * List Discovered Items
     */
    getDiscoveryItems({
        uuid,
        resource,
        newlyDiscovered,
        itemsPerPage,
        pageNumber,
    }: GetDiscoveryItemsRequest): Observable<PaginationResponseDtoDiscoveryItemDto>;
    getDiscoveryItems(
        { uuid, resource, newlyDiscovered, itemsPerPage, pageNumber }: GetDiscoveryItemsRequest,
        opts?: OperationOpts,
    ): Observable<AjaxResponse<PaginationResponseDtoDiscoveryItemDto>>;
    getDiscoveryItems(
        { uuid, resource, newlyDiscovered, itemsPerPage, pageNumber }: GetDiscoveryItemsRequest,
        opts?: OperationOpts,
    ): Observable<PaginationResponseDtoDiscoveryItemDto | AjaxResponse<PaginationResponseDtoDiscoveryItemDto>> {
        throwIfNullOrUndefined(uuid, 'uuid', 'getDiscoveryItems');

        const queryParams: HttpQuery = {};

        if (resource != null) {
            queryParams['resource'] = resource;
        }
        if (newlyDiscovered != null) {
            queryParams['newlyDiscovered'] = newlyDiscovered;
        }
        if (itemsPerPage != null) {
            queryParams['itemsPerPage'] = itemsPerPage;
        }
        if (pageNumber != null) {
            queryParams['pageNumber'] = pageNumber;
        }

        return this.request<PaginationResponseDtoDiscoveryItemDto>(
            {
                url: '/v1/discoveries/{uuid}/items'.replace('{uuid}', encodeURI(uuid)),
                method: 'GET',
                queryParams,
            },
            opts?.responseOpts,
        );
    }

    /**
     * Relays the attribute definitions that refine discovery of one resource type from the Connector\'s discovery interface.
     * Get per-resource Discovery Attributes from a Discovery Provider
     */
    getDiscoveryResourceAttributes({ connectorUuid, resource }: GetDiscoveryResourceAttributesRequest): Observable<Array<BaseAttributeDto>>;
    getDiscoveryResourceAttributes(
        { connectorUuid, resource }: GetDiscoveryResourceAttributesRequest,
        opts?: OperationOpts,
    ): Observable<AjaxResponse<Array<BaseAttributeDto>>>;
    getDiscoveryResourceAttributes(
        { connectorUuid, resource }: GetDiscoveryResourceAttributesRequest,
        opts?: OperationOpts,
    ): Observable<Array<BaseAttributeDto> | AjaxResponse<Array<BaseAttributeDto>>> {
        throwIfNullOrUndefined(connectorUuid, 'connectorUuid', 'getDiscoveryResourceAttributes');
        throwIfNullOrUndefined(resource, 'resource', 'getDiscoveryResourceAttributes');

        return this.request<Array<BaseAttributeDto>>(
            {
                url: '/v1/discoveries/{connectorUuid}/{resource}/attributes'
                    .replace('{connectorUuid}', encodeURI(connectorUuid))
                    .replace('{resource}', encodeURI(resource)),
                method: 'GET',
            },
            opts?.responseOpts,
        );
    }

    /**
     * Returns one page of the advisory messages a Discovery run collected, oldest first and stable across pages. Repeated problems are aggregated into a single entry carrying an occurrence count rather than repeated per occurrence. Messages are advisory: collecting them does not imply the run failed.
     * List Discovery Run Messages
     */
    getDiscoveryRunMessages({
        uuid,
        itemsPerPage,
        pageNumber,
    }: GetDiscoveryRunMessagesRequest): Observable<PaginationResponseDtoDiscoveryMessageDto>;
    getDiscoveryRunMessages(
        { uuid, itemsPerPage, pageNumber }: GetDiscoveryRunMessagesRequest,
        opts?: OperationOpts,
    ): Observable<AjaxResponse<PaginationResponseDtoDiscoveryMessageDto>>;
    getDiscoveryRunMessages(
        { uuid, itemsPerPage, pageNumber }: GetDiscoveryRunMessagesRequest,
        opts?: OperationOpts,
    ): Observable<PaginationResponseDtoDiscoveryMessageDto | AjaxResponse<PaginationResponseDtoDiscoveryMessageDto>> {
        throwIfNullOrUndefined(uuid, 'uuid', 'getDiscoveryRunMessages');

        const queryParams: HttpQuery = {};

        if (itemsPerPage != null) {
            queryParams['itemsPerPage'] = itemsPerPage;
        }
        if (pageNumber != null) {
            queryParams['pageNumber'] = pageNumber;
        }

        return this.request<PaginationResponseDtoDiscoveryMessageDto>(
            {
                url: '/v1/discoveries/{uuid}/messages'.replace('{uuid}', encodeURI(uuid)),
                method: 'GET',
                queryParams,
            },
            opts?.responseOpts,
        );
    }

    /**
     * Besides the conditions a field may be filtered with, each field reports whether it can serve as a configurable column of the listing: `displayable` marks the fields that may be named in `columns`, and `sortable` those the listing may be ordered by. A field that reports neither flag is filter-only, so an absent flag is to be read as `false` rather than as unknown.  Both flags are answered per field, and that answer is authoritative for attribute-sourced fields as much as for property ones: whether a given attribute may be ordered on depends on the resource and is reported here rather than assumed. A field that may be shown but not ordered on reports `displayable` without `sortable`.
     * Get Discovery searchable fields information
     */
    getDiscoverySearchableFields(): Observable<Array<SearchFieldDataByGroupDto>>;
    getDiscoverySearchableFields(opts?: OperationOpts): Observable<AjaxResponse<Array<SearchFieldDataByGroupDto>>>;
    getDiscoverySearchableFields(
        opts?: OperationOpts,
    ): Observable<Array<SearchFieldDataByGroupDto> | AjaxResponse<Array<SearchFieldDataByGroupDto>>> {
        return this.request<Array<SearchFieldDataByGroupDto>>(
            {
                url: '/v1/discoveries/search',
                method: 'GET',
            },
            opts?.responseOpts,
        );
    }

    /**
     * Ordering and columns address a field by its source together with its identifier, because an identifier is unique only within its source. Both halves, and which fields may be shown or ordered on, come from the searchable-fields operation of this resource.  `sort` orders the whole result set before it is paged, so paging walks the sorted set rather than sorting one page at a time; only fields the catalogue marks `sortable` may be used. `columns` names the fields the caller means to display, and only fields the catalogue marks `displayable` may be named. It does not narrow the response: every listing object comes back whole, and naming a property field asks for nothing extra because the object already carries it. Naming an attribute-sourced field is what has an effect, described below.  A request that carries neither `sort` nor `columns` is answered exactly as it was before the two fields existed: the endpoint\'s own default ordering, the full default shape of every object, and no `attributeValues` member. A caller written against the previous contract therefore needs no change.  Requesting attribute-sourced columns adds an `attributeValues` member to each returned object, keyed by field source and then by field identifier. A field the object holds no value for is absent rather than empty, and a multi-valued attribute arrives in its stored `item_order`.
     * List Discovery
     */
    listDiscoveries({ searchRequestDto }: ListDiscoveriesRequest): Observable<DiscoveryResponseDto>;
    listDiscoveries({ searchRequestDto }: ListDiscoveriesRequest, opts?: OperationOpts): Observable<AjaxResponse<DiscoveryResponseDto>>;
    listDiscoveries(
        { searchRequestDto }: ListDiscoveriesRequest,
        opts?: OperationOpts,
    ): Observable<DiscoveryResponseDto | AjaxResponse<DiscoveryResponseDto>> {
        throwIfNullOrUndefined(searchRequestDto, 'searchRequestDto', 'listDiscoveries');

        const headers: HttpHeaders = {
            'Content-Type': 'application/json',
        };

        return this.request<DiscoveryResponseDto>(
            {
                url: '/v1/discoveries/list',
                method: 'POST',
                headers,
                body: searchRequestDto,
            },
            opts?.responseOpts,
        );
    }

    /**
     * Returns the resource types this Connector\'s discovery interface supports, relayed live from the Connector. For a Connector implementing only the v1 discovery interface, Core synthesizes the single entry \"certificates\" without calling the Connector — a client renders one shape for both generations.
     * Get discoverable resources of a Discovery Provider
     */
    listDiscoveryResources({ connectorUuid }: ListDiscoveryResourcesRequest): Observable<Array<DiscoverySupportedResourceDto>>;
    listDiscoveryResources(
        { connectorUuid }: ListDiscoveryResourcesRequest,
        opts?: OperationOpts,
    ): Observable<AjaxResponse<Array<DiscoverySupportedResourceDto>>>;
    listDiscoveryResources(
        { connectorUuid }: ListDiscoveryResourcesRequest,
        opts?: OperationOpts,
    ): Observable<Array<DiscoverySupportedResourceDto> | AjaxResponse<Array<DiscoverySupportedResourceDto>>> {
        throwIfNullOrUndefined(connectorUuid, 'connectorUuid', 'listDiscoveryResources');

        return this.request<Array<DiscoverySupportedResourceDto>>(
            {
                url: '/v1/discoveries/{connectorUuid}/resources'.replace('{connectorUuid}', encodeURI(connectorUuid)),
                method: 'GET',
            },
            opts?.responseOpts,
        );
    }

    /**
     * Asks the Discovery Provider to continue a stopped run from its checkpoint. Legal only from the stopped status. A Provider that has since lost the checkpoint fails the run instead of restarting it, so a resume is never silently a fresh scan.
     * Resume Discovery
     */
    resumeDiscovery({ uuid }: ResumeDiscoveryRequest): Observable<void>;
    resumeDiscovery({ uuid }: ResumeDiscoveryRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>>;
    resumeDiscovery({ uuid }: ResumeDiscoveryRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>> {
        throwIfNullOrUndefined(uuid, 'uuid', 'resumeDiscovery');

        return this.request<void>(
            {
                url: '/v1/discoveries/{uuid}/resume'.replace('{uuid}', encodeURI(uuid)),
                method: 'PATCH',
            },
            opts?.responseOpts,
        );
    }

    /**
     * Schedule Discovery
     */
    scheduleDiscovery({ scheduleDiscoveryDto }: ScheduleDiscoveryRequest): Observable<UuidDto>;
    scheduleDiscovery({ scheduleDiscoveryDto }: ScheduleDiscoveryRequest, opts?: OperationOpts): Observable<AjaxResponse<UuidDto>>;
    scheduleDiscovery(
        { scheduleDiscoveryDto }: ScheduleDiscoveryRequest,
        opts?: OperationOpts,
    ): Observable<UuidDto | AjaxResponse<UuidDto>> {
        throwIfNullOrUndefined(scheduleDiscoveryDto, 'scheduleDiscoveryDto', 'scheduleDiscovery');

        const headers: HttpHeaders = {
            'Content-Type': 'application/json',
        };

        return this.request<UuidDto>(
            {
                url: '/v1/discoveries/schedule',
                method: 'POST',
                headers,
                body: scheduleDiscoveryDto,
            },
            opts?.responseOpts,
        );
    }

    /**
     * Asks the Discovery Provider to suspend an in-progress run, keeping everything already staged and the Provider-side checkpoint needed to resume it. Legal only while the run is in progress.
     * Stop Discovery
     */
    stopDiscovery({ uuid }: StopDiscoveryRequest): Observable<void>;
    stopDiscovery({ uuid }: StopDiscoveryRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>>;
    stopDiscovery({ uuid }: StopDiscoveryRequest, opts?: OperationOpts): Observable<void | AjaxResponse<void>> {
        throwIfNullOrUndefined(uuid, 'uuid', 'stopDiscovery');

        return this.request<void>(
            {
                url: '/v1/discoveries/{uuid}/stop'.replace('{uuid}', encodeURI(uuid)),
                method: 'PATCH',
            },
            opts?.responseOpts,
        );
    }
}
